# EDIL BINGO monorepo — copy to .env for local dev. Never commit .env files.
# Railway: set all values in the service Variables tab.

# --- Required ---
DATABASE_URL=
GAME_BOT_TOKEN=
WITHDRAW_BOT_TOKEN=
ADMIN_CHAT_ID=

# Aliases also supported: BOT_TOKEN, MONGO_URI, MONGODB_URI

# --- Server ---
PORT=3001
NODE_ENV=production

# --- Telegram Mini App ---
MINI_APP_URL=

# --- Admin panel / API auth ---
ADMIN_TOKEN=
ADMIN_PASSWORD=

# --- CORS (comma-separated; include your Railway public URL in production) ---
CORS_ORIGIN=

# --- Verifier API (payment receipt verification) ---
# Started automatically as a child process when dist exists.
VERIFIER_API_PORT=3002
VERIFIER_API_URL=
VERIFIER_API_KEY=
START_VERIFIER_API=true
DISABLE_VERIFIER_API=false

# --- Monorepo orchestrator ---
SERVE_MONOREPO_STATIC=true
DISABLE_TELEGRAM_BOTS=false
SKIP_MONOREPO_BUILD=false
BUILD_MONOREPO=false
# Root npm install uses workspaces; postinstall builds only (no nested npm install).

# --- Optional MongoDB tuning ---
MONGO_URI_DIRECT=
MONGO_CONNECT_RETRIES=5
