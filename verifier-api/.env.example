# Verifier API — inherits parent process.env when spawned by backend orchestrator.
# For standalone dev, copy to verifier-api/.env (never commit).
#
# Production VPS base URL (Ethiopian hosting):
# http://196.189.51.146:3002
# Set VERIFIER_API_URL in the backend/Railway env to this base when verifier runs on VPS.

DATABASE_URL=
PORT=3002
API_KEY=
