# Dev: leave unset to use Vite proxy (/api -> http://localhost:3001)
# VITE_API_URL=/api

# Production: full backend API base including /api
# VITE_API_URL=https://your-backend.onrender.com/api

# Socket.io (optional — defaults to same origin as admin panel)
# VITE_SOCKET_URL=
